# Online Book Store

A Vite-powered React bookstore assignment with reusable components, separate CSS files, category filters, and title search.

## Setup

```bash
npm install
npm run dev
```

Create a production build with `npm run build`.
